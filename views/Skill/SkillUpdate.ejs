<!DOCTYPE html>
<html>
<head>
    <title>Skill - ADD</title>
</head>
<body>
<h1>Add New Skill</h1>
<form action="/skill/update" method="get">
        Skill Id: <input id="skill_id"
        name="skill_id"
        type="text"
        value="<%= skill_result.skill_id%>"
    />
    <br />
            <% for(var i=0; skill_result.length > i; i++ ) {
                var checked ="";

                if(skill_result[i].skill_id != null) {
                    checked = "checked";
                }
            %>
    <input type="checkbox" name="skill_id" <%= checked %> value="<%= skill_result[i].skill_id %>"> <%= address_result[i].street + ',' +
         skill_result[i].skill_description %>
        <br />
        <% } %>
    <input type="submit" />
    <input type="hidden"
        id="skill_id"
        name="skill_id"
        value="<%= skill.skill_id %>"
</form>
</body>
</html>